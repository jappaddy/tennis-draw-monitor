LINE_CHANNEL_ACCESS_TOKEN=your_line_messaging_api_channel_access_token
LINE_USER_ID=your_line_user_id
# DRY_RUN=true          # trueにするとLINE送信とstate.json書き込みをスキップ（ローカル確認用）
# FORCE_RUN_ALL=true    # trueにするとcheck_interval_minutesを無視して全対象を強制チェック
# TARGET_NAME=大会名     # 指定した名前の対象のみチェック
